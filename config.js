const config = {
  db: {
    host: "localhost",
    user: "root",
    password: "12345",
    database: "ecotrack",
    connectTimeout: 60000
  },
  // listPerPage: 10,
};

module.exports ={
  config
}
